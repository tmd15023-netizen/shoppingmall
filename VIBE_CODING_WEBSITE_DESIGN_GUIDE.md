# 바이브코딩으로 웹사이트 설계하기 — 순서 & 용어 가이드

이 문서는 **바이브코딩(Vibe Coding)** 으로 웹사이트를 설계·구현할 때  
**어떤 순서로 생각하고, 어떤 용어를 알아야 하는지**를  
웹사이트 설계 흐름에 맞춰 상세히 정리한 가이드입니다.

앞서 만든 쇼핑몰(`shop-demo`) 경험과도 연결해 두었습니다.

---

## 이 문서의 읽는 법

| 표시 | 의미 |
|------|------|
| **[필수]** | 이 단계를 건너뛰면 나중에 큰 수정이 생김 |
| **[권장]** | 품질·속도가 크게 좋아짐 |
| **[선택]** | 규모가 커질 때 필요 |
| **용어** | 그 단계에서 꼭 알아둘 말 |

> **바이브코딩이란?**  
> 완벽한 설계 문서를 먼저 다 쓰기보다,  
> **원하는 느낌(vibe)·목표·화면을 AI에게 설명하며**  
> 대화로 코드를 만들고 다듬는 개발 방식입니다.  
> 다만 “느낌만”으로 가면 망합니다.  
> **순서를 지키면서 바이브로 구현**하는 것이 핵심입니다.

---

# 전체 순서 한눈에 보기

```text
1. 목적·타깃 잡기          (왜, 누구를 위한 사이트인가)
2. 기능 범위 정하기        (필수 / 나중에)
3. 정보구조·동선 잡기      (페이지와 이동 흐름)
4. 화면(와이어/레퍼런스)   (대략적인 UI 스케치)
5. 기술 스택 고르기        (프론트/백/DB/배포)
6. 데이터·권한 설계        (무엇을 저장하고 누가 보나)
7. AI와 구현 시작          (바이브코딩 본게임)
8. 연동·환경변수           (외부 서비스, .env)
9. 테스트·다듬기           (깨진 흐름 고치기)
10. 배포·운영              (인터넷에 올리기)
11. 개선 반복              (피드백 → 다음 바이브)
```

아래부터 **순서대로** 용어와 실전 팁을 설명합니다.

---

# 1단계. 목적과 타깃 잡기

## 무엇을 하나?
“예쁜 사이트”보다 먼저 **이 사이트가 성공한 순간**을 문장으로 적습니다.

예시 (쇼핑몰):
> 방문자가 상품을 고르고, 로그인 후 결제까지 끝낸다.

## [필수] 적을 것
1. **한 줄 목적** (이 사이트의 존재 이유)
2. **주요 사용자** (손님 / 관리자 / 비회원…)
3. **성공 기준** (가입 완료, 결제 완료, 문의 접수 등)
4. **하지 않을 것** (이번엔 소셜로그인 안 함 등)

## 이 단계 용어

| 용어 | 쉬운 설명 |
|------|-----------|
| **프로덕트 (Product)** | 만들고 있는 서비스/웹사이트 자체 |
| **유저 / 타깃 유저** | 실제로 쓸 사람 |
| **페르소나** | “30대 직장인 구매자”처럼 대표 사용자 상 |
| **유스케이스 (Use Case)** | 사용자가 이루려는 구체적 일 (예: 장바구니에 담기) |
| **MVP** | Minimum Viable Product. 최소로 동작하는 첫 버전 |
| **스코프 (Scope)** | 이번 버전에 넣을 일의 범위 |
| **바이브 (Vibe)** | 사이트의 분위기·톤 (미니멀, 럭셔리, 캐주얼 등) |

## 바이브코딩 프롬프트 예시
```text
타깃은 20~30대 여성 패션 구매자야.
톤은 깔끔하고 밝고, 브랜드명은 SSEUNG.
이번 MVP는 상품조회·장바구니·결제·관리자 상품등록까지야.
소셜로그인은 이번엔 제외해.
```

---

# 2단계. 기능 범위 나누기 (필수 vs 나중)

## 무엇을 하나?
하고 싶은 기능을 전부 적고, **지금 / 나중**으로 나눕니다.

## [필수] 분류 방법

| 구분 | 기준 | 예 |
|------|------|----|
| **Must (필수)** | 없으면 서비스가 성립 안 됨 | 로그인, 상품, 결제 |
| **Should (권장)** | 있으면 훨씬 좋음 | 주문상세, 카테고리 필터 |
| **Could (선택)** | 나중에 | 리뷰 실데이터, 쿠폰 |
| **Won't (이번엔 안 함)** | 명시적으로 제외 | 소셜로그인 |

## 이 단계 용어

| 용어 | 쉬운 설명 |
|------|-----------|
| **기능 요구사항** | “무엇을 할 수 있어야 한다” 목록 |
| **비기능 요구사항** | 속도, 보안, 모바일 대응 같은 품질 조건 |
| **우선순위** | 무엇을 먼저 할지 |
| **백로그** | 앞으로 할 일 목록 |
| **의존성** | A를 하려면 B가 먼저 필요함 (결제 ← 장바구니 ← 로그인) |

## [주의]
바이브코딩에서 제일 흔한 실패:
> “일단 다 만들어줘”

→ AI가 겉만 번지르르하고 **속은 빈 화면**을 만들기 쉽습니다.  
**필수 기능 5~8개**만 먼저 고정하세요.

---

# 3단계. 정보구조와 사용자 동선

## 무엇을 하나?
사이트를 **페이지 단위**로 쪼개고, 사용자가 어떻게 이동하는지 그립니다.

## [필수] 쇼핑몰 예시 동선

```text
홈 → 상품상세 → 장바구니 → 결제 → 주문완료
         ↘ 로그인/회원가입
관리자 → 상품등록/주문관리
```

## [권장] 페이지 목록표 만들기

| 페이지 | URL 예 | 누가 쓰나 | 핵심 행동 |
|--------|--------|-----------|-----------|
| 홈 | `/` | 모두 | 상품 구경 |
| 상세 | `/product/:id` | 모두 | 옵션 선택·담기 |
| 장바구니 | `/cart` | 회원 | 수량 변경 |
| 결제 | `/checkout` | 회원 | 결제 |
| 관리자 | `/admin` | admin | 상품/주문 관리 |

## 이 단계 용어

| 용어 | 쉬운 설명 |
|------|-----------|
| **IA (Information Architecture)** | 정보구조. 메뉴·페이지를 어떻게 나눌지 |
| **사이트맵** | 페이지 전체 지도 |
| **유저 플로우 (User Flow)** | 사용자가 클릭하며 가는 길 |
| **라우트 (Route)** | 주소와 화면의 연결 (`/cart` → 장바구니 페이지) |
| **네비게이션** | 메뉴, 헤더, 뒤로가기 등 이동 수단 |
| **CTA** | Call To Action. “구매하기”처럼 핵심 버튼 |
| **진입점** | 사용자가 처음 들어오는 화면 (보통 홈) |
| **이탈** | 중간에 나가버리는 것 (결제 전 이탈 등) |

## 바이브코딩 팁
AI에게 코드 시키기 전에 이렇게 말하세요.

```text
페이지는 Home, ProductDetail, Cart, Checkout, Login, Admin 이야.
URL도 같이 정해줘. 아직 스타일은 말고 라우팅과 빈 페이지만.
```

**화면 예쁘게**보다 **길부터** 만드는 것이 바이브코딩 성공률을 올립니다.

---

# 4단계. 화면 설계 (와이어프레임 → UI 톤)

## 무엇을 하나?
픽셀 완벽한 디자인보다, **어디에 무엇이 있는지**를 먼저 정합니다.

## 4-1. [필수] 와이어프레임
손그림/메모로 OK.

각 페이지마다:
- 헤더에 뭐가 있나
- 메인에 뭐가 있나
- 버튼이 어디 있나
- 로그인 전/후가 어떻게 다르나

## 4-2. [권장] 레퍼런스 & 바이브
- 참고 사이트 2~3개
- 색/폰트/여백 느낌 문장으로 정의

예:
> 화이트 배경, 큰 타이포, 카드보다 풀블리드 히어로, 과한 보라 그라데이션 금지

## 4-3. [선택] 디자인 시스템
버튼, 입력창, 색 변수를 통일.

## 이 단계 용어

| 용어 | 쉬운 설명 |
|------|-----------|
| **와이어프레임** | 뼈대 스케치 (색/이미지 없이 구조만) |
| **목업 (Mockup)** | 색·이미지 넣은 정적 시안 |
| **프로토타입** | 클릭해보는 흉내 버전 |
| **UI** | User Interface. 눈에 보이는 화면 |
| **UX** | User Experience. 쓰면서 느끼는 경험 전체 |
| **레이아웃** | 배치 구조 |
| **히어로** | 첫 화면 큰 비주얼/카피 영역 |
| **컴포넌트** | 버튼·카드·네비처럼 재사용하는 UI 조각 |
| **반응형** | 모바일/데스크톱에서 깨지지 않게 대응 |
| **브레이크포인트** | 화면 너비 기준점 (예: 768px) |
| **접근성 (A11y)** | 누구나 쓰기 쉽게 (대비, 키보드, alt 등) |
| **디자인 토큰** | 색/간격/폰트를 변수로 통일한 값 |

## 바이브코딩 팁
디자인부터 완벽히 하지 마세요.  
대신 AI에게:

```text
먼저 레이아웃만. 카드 남발하지 말고, 히어로는 풀폭.
브랜드명 SSEUNG이 첫 화면에서 잘 보이게.
모바일에서도 읽히게.
```

이미지나 스크린샷을 주면 더 잘 맞춥니다.

---

# 5단계. 기술 스택 고르기

## 무엇을 하나?
“어떤 도구로 만들지”를 고릅니다.  
바이브코딩도 **스택이 정해져야** AI가 헤매지 않습니다.

## [필수] 최소 결정 4가지

1. **프론트엔드** (화면) — 예: React + Vite  
2. **백엔드** (서버/API) — 예: Node + Express  
3. **데이터베이스** — 예: MongoDB Atlas  
4. **배포** — 예: Vercel(프론트) + Heroku(API)

정적 소개 페이지만이면 백엔드/DB 없이 가능합니다.  
**로그인·결제·주문**이 있으면 백엔드/DB가 **[필수]**입니다.

## 이 단계 용어

| 용어 | 쉬운 설명 |
|------|-----------|
| **프론트엔드** | 브라우저에서 도는 화면 코드 |
| **백엔드** | 서버에서 도는 비즈니스/데이터 코드 |
| **풀스택** | 프론트+백엔드를 함께 다룸 |
| **프레임워크** | 개발을 도와주는 틀 (React, Express 등) |
| **라이브러리** | 가져다 쓰는 코드 묶음 |
| **스택 (Stack)** | 사용하는 기술 조합 |
| **SPA** | Single Page Application. 페이지 전환이 부드러운 앱형 웹 |
| **SSR / SSG** | 서버에서 HTML을 만들어 주는 방식 / 미리 만들어 두는 방식 |
| **API** | 프론트가 서버에 요청하는 약속된 창구 |
| **REST API** | URL+HTTP 메서드로 자원을 다루는 흔한 API 방식 |
| **JSON** | 데이터 주고받는 텍스트 형식 |
| **모노레포** | 프론트/서버를 한 저장소에 두는 방식 |

## 쇼핑몰에 맞춘 실전 선택 (참고)
- 화면: React  
- 서버: Express  
- DB: MongoDB  
- 이미지: Cloudinary  
- 결제: PortOne  
- 배포: GitHub + Vercel + Heroku  

---

# 6단계. 데이터·권한·상태 설계

## 무엇을 하나?
화면에 보이는 정보가 **어디서 오고, 누가 수정 가능한지**를 정합니다.

## 6-1. [필수] 데이터 목록
예:
- User (회원)
- Product (상품)
- Cart (장바구니)
- Order (주문)

각 항목의 필드(이름, 가격, 이미지…)를 대략 적습니다.

## 6-2. [필수] 권한
| 역할 | 할 수 있는 일 |
|------|----------------|
| 비회원 | 상품 보기 |
| 회원 | 장바구니, 주문 |
| 관리자 | 상품/주문 관리 |

## 6-3. [권장] 상태
주문 상태 예: `결제완료 → 배송중 → 배송완료 / 취소`

## 이 단계 용어

| 용어 | 쉬운 설명 |
|------|-----------|
| **데이터 모델 / 스키마** | 저장 데이터의 뼈대 |
| **컬렉션 / 테이블** | 데이터 묶음 단위 |
| **CRUD** | Create/Read/Update/Delete (생성·조회·수정·삭제) |
| **인증 (Authentication)** | 네가 누구인지 확인 (로그인) |
| **인가 (Authorization)** | 그 일을 할 권한이 있는지 |
| **세션** | 로그인 상태 유지 방식 중 하나 |
| **JWT** | 토큰으로 로그인 상태를 증명하는 방식 |
| **미들웨어** | 요청이 본 로직에 가기 전 거쳐가는 검사층 |
| **유효성 검사 (Validation)** | 입력값이 올바른지 확인 |
| **상태 (State)** | 화면/서버가 기억하고 있는 현재 값 |
| **전역 상태** | 여러 페이지가 공유하는 상태 (로그인 유저, 장바구니) |

## 바이브코딩 팁
```text
먼저 User/Product/Cart/Order 모델과
회원/관리자 권한부터 만들어줘.
UI는 최소로, API가 먼저야.
```

**예쁜 관리자 페이지보다 DB와 권한**이 먼저입니다.

---

# 7단계. 바이브코딩으로 구현하기 (본게임)

## 원칙
한 번에 “쇼핑몰 전부”가 아니라 **세로 슬라이스**로 만듭니다.

좋은 슬라이스 예:
1. 상품 목록 API + 홈 목록
2. 로그인 + 네비바 이름 표시
3. 장바구니 담기/조회
4. 결제
5. 관리자 상품 등록

## [필수] AI에게 시킬 때의 순서

1. **목표** (이번엔 뭐 완성?)
2. **범위** (하지 말 것)
3. **현재 스택/폴더**
4. **성공 기준** (어떻게 되면 끝난 건지)
5. **제약** (디자인 톤, 보안, 기존 코드 유지)

### 좋은 프롬프트 예시
```text
지금 스택은 React+Express+MongoDB야.
이번 작업은 장바구니만.
로그인 유저만 담을 수 있게 하고,
네비바 배지 숫자도 갱신해줘.
결제/관리자는 건드리지 마.
끝나면 로컬에서 담기→장바구니 페이지 확인 방법으로 알려줘.
```

### 나쁜 프롬프트 예시
```text
쇼핑몰 예쁘게 완성해줘.
```

## 이 단계 용어

| 용어 | 쉬운 설명 |
|------|-----------|
| **프롬프트** | AI에게 주는 지시문 |
| **컨텍스트** | AI가 참고하는 현재 코드/상황 |
| **리팩터링** | 동작은 유지한 채 코드 구조 정리 |
| **버그** | 의도대로 안 도는 결함 |
| **핫픽스** | 급하게 고치는 수정 |
| **컴포넌트 분리** | 큰 UI를 작은 단위로 나누기 |
| **프록시** | 로컬에서 프론트→백엔드 요청을 대신 전달 |
| **CORS** | 다른 주소(프론트/백) 간 요청 허용 규칙 |
| **에러 핸들링** | 실패했을 때 사용자/개발자에게 알리는 처리 |
| **로그** | 서버/앱이 남기는 실행 기록 |
| **Lorem/더미데이터** | 임시 가짜 데이터 |

## [권장] 바이브코딩 루프

```text
요청 → 생성 → 실행 → 확인 → 피드백 → 수정 → 다음 슬라이스
```

확인 없이 다음 기능으로 넘어가면 부채가 쌓입니다.

---

# 8단계. 외부 연동과 환경변수

## 무엇을 하나?
이미지, 결제, 이메일처럼 **남이 제공하는 서비스**를 붙입니다.

## [필수] 환경변수 개념
비밀값/설정값을 코드에 직접 쓰지 않고 `.env`에 둡니다.

| 위치 | 예시 | 공개?
|------|------|------|
| 서버 `.env` | DB주소, JWT비밀, 결제 Secret | ❌ 비공개 |
| 클라이언트 `VITE_` | API주소, Cloudinary cloud name | 빌드에 포함될 수 있음 (비밀키 금지) |

## 이 단계 용어

| 용어 | 쉬운 설명 |
|------|-----------|
| **환경변수** | 실행 환경마다 달라지는 설정값 |
| **`.env`** | 환경변수를 모아둔 파일 |
| **시크릿** | 비밀번호·API Secret 등 비밀 |
| **SDK** | 서비스가 제공하는 개발 도구 키트 |
| **웹훅 (Webhook)** | 외부 서비스가 우리 서버로 이벤트 알려주는 방식 |
| **콜백** | 작업 후 다시 호출되는 함수/URL |
| **업로드 프리셋** | Cloudinary에서 업로드 규칙을 미리 정해둔 설정 |
| **도메인 허용** | 결제/로그인 등에서 허용할 사이트 주소 등록 |
| **레이트 리밋** | 요청 횟수 제한 |

## [주의]
- `.env`는 Git에 올리지 않기
- 프론트에 **Secret Key** 넣지 않기
- 배포 환경(Vercel/Heroku)에도 같은 키를 다시 등록

---

# 9단계. 테스트와 품질 다듬기

## 무엇을 하나?
“일단 된다”에서 “사람이 써도 된다”로 올립니다.

## [필수] 수동 테스트 시나리오
체크리스트로 클릭해봅니다.

1. 비회원: 상품 보이는지
2. 회원가입/로그인
3. 장바구니 담기
4. 결제 성공/실패
5. 관리자 상품 등록
6. 모바일 화면

## [권장] 고치면 좋은 것
- 로딩 중 표시
- 에러 메시지 한국어로 명확히
- 빈 목록(장바구니 비움) UI
- 로그인 만료 시 안내

## 이 단계 용어

| 용어 | 쉬운 설명 |
|------|-----------|
| **QA** | 품질 확인 활동 |
| **테스트 케이스** | “이런 입력 → 이런 결과” 검증 항목 |
| **회귀 버그** | 고치다가 예전에 되던 게 다시 깨짐 |
| **엣지 케이스** | 극단적 상황 (수량 0, 긴 텍스트, 느린 네트워크) |
| **반응형 깨짐** | 모바일에서 레이아웃 붕괴 |
| **콘솔** | 브라우저 개발자도구의 에러/로그 창 |
| **네트워크 탭** | API 요청/응답을 보는 곳 |
| **성능** | 로딩 속도, 버벅임 |
| **보안** | 해킹·정보유출 방지 기본 (인증, 시크릿 관리) |

## 바이브코딩 팁
버그를 말할 때 이렇게:

```text
증상: 로그인은 되어 보이는데 장바구니 담기 시 로그인 필요 알림
재현: 상품상세 → 옵션선택 → 장바구니 클릭
기대: 담기기
실제: alert 발생
가능하면 관련 파일도 같이 봐줘
```

AI는 **모호한 불만**보다 **재현 절차**에 강합니다.

---

# 10단계. 배포와 공개

## 무엇을 하나?
내 PC가 아니라 **인터넷 주소**로 접속되게 합니다.

## [필수] 전형적인 순서
1. GitHub에 코드 푸시
2. 백엔드 배포 (예: Heroku) → API 주소 확보
3. 프론트 배포 (예: Vercel) → `VITE_API_URL`에 API 주소
4. CORS/`CLIENT_ORIGIN`으로 서로 허용
5. 실제 도메인에서 결제/업로드 재확인

## 이 단계 용어

| 용어 | 쉬운 설명 |
|------|-----------|
| **배포 (Deploy)** | 서버에 올려 공개하는 것 |
| **호스팅** | 사이트를 대신 실행해 주는 서비스 |
| **도메인** | `example.com` 같은 주소 |
| **DNS** | 도메인 ↔ 서버 연결 정보 |
| **빌드** | 배포용 파일로 변환하는 과정 |
| **프로덕션** | 실제 사용자용 환경 |
| **스테이징/프리뷰** | 미리보기용 임시 환경 |
| **CI/CD** | 푸시하면 자동 테스트/배포되는 파이프라인 |
| **환경 (Environment)** | local / preview / production |
| **Dyno/인스턴스** | 서버가 실제로 실행되는 단위 (Heroku 등) |
| **HTTPS** | 암호화된 접속 (자물쇠) |
| **롤백** | 이전 버전으로 되돌리기 |

## [주의]
- 로컬에서 된다고 배포에서 되는 것은 아님
- 환경변수 누락이 배포 실패 1위
- 프론트 `VITE_` 변경 후엔 **재빌드/재배포** 필요

---

# 11단계. 개선 반복 (바이브 유지보수)

## 무엇을 하나?
출시 후 피드백을 다음 작업으로 바꿉니다.

## [권장] 개선 사이클
```text
관찰(불편/에러) → 우선순위 → 작은 수정 → 배포 → 다시 관찰
```

## 이 단계 용어

| 용어 | 쉬운 설명 |
|------|-----------|
| **피드백** | 사용자/본인 테스트에서 나온 의견 |
| **이슈** | 해결할 문제 단위 |
| **커밋** | 변경 이력을 남기는 저장 단위 |
| **푸시** | 원격(GitHub)에 업로드 |
| **풀 리퀘스트 (PR)** | 코드 변경 검토/병합 요청 |
| **버전** | 배포된 코드의 세대 |
| **기술 부채** | 나중에 갚아야 할 엉성한 코드/구조 |
| **모니터링** | 장애·트래픽을 지켜보는 것 |
| **분석 (Analytics)** | 방문/클릭/전환 수치 보기 |
| **전환율** | 방문 대비 구매 등 목표 달성 비율 |

---

# 실전 요약: “설계 순서별 필수 산출물”

바이브코딩을 해도, 각 단계 끝에 **이것만큼은** 남겨 두세요.

| 단계 | [필수] 산출물 |
|------|----------------|
| 1. 목적 | 한 줄 목적 + 타깃 + 성공 기준 |
| 2. 범위 | Must/Should/Won't 목록 |
| 3. 동선 | 페이지 목록 + URL + 유저 플로우 |
| 4. 화면 | 페이지별 와이어(손그림 OK) + 바이브 문장 |
| 5. 스택 | 프론트/백/DB/배포 결정 |
| 6. 데이터 | 모델 목록 + 역할(권한) |
| 7. 구현 | 슬라이스 단위 완료 정의 |
| 8. 연동 | 사용할 외부서비스 + env 목록 |
| 9. 테스트 | 클릭 체크리스트 |
| 10. 배포 | 배포 URL + env 연결 확인 |
| 11. 개선 | 다음 주 할 일 3개 |

---

# 바이브코딩 전용: 역할 분담

| 사람(당신) | AI |
|------------|----|
| 목적/우선순위 결정 | 코드 작성·수정 |
| 뭐가 맞는지 판정 | 여러 구현 옵션 제시 |
| 실제 클릭 테스트 | 반복 작업 자동화 |
| 보안·비밀값 관리 | 파일 구조 제안 |
| 최종 책임 | 설명/문서 초안 |

> AI는 빠른 주니어 개발자처럼 쓰되,  
> **제품 결정권은 항상 당신에게** 두세요.

---

# 웹사이트 유형별 최소 설계 차이

| 유형 | 최소 필수 | 백엔드 필요? |
|------|-----------|--------------|
| 소개/포트폴리오 | 페이지, 연락처, 반응형 | 보통 선택 |
| 블로그 | 글 목록/상세, 관리 | 권장 |
| 커뮤니티 | 회원, 글, 권한 | 필수 |
| 쇼핑몰 | 상품, 회원, 장바구니, 결제, 주문 | **필수** |
| SaaS 대시보드 | 로그인, 권한, 데이터 화면 | **필수** |

---

# 초보자가 바이브코딩할 때 자주 하는 실수

1. **디자인부터 완벽히** 만들려다 동선이 없음  
2. **필수/선택 미구분**으로 끝없이 추가  
3. **프론트만** 만들고 로그인이 “된 척”  
4. **환경변수/배포**를 마지막에야 생각  
5. **에러 메시지 없이** “안 돼요”만 반복  
6. **한 대화에 너무 많은 요청**  
7. **Git에 .env 업로드**  
8. **동작 확인 없이** 다음 기능으로 점프  

---

# 바로 쓰는 템플릿

## A. 프로젝트 시작 브리프
```text
[목적]
[타깃]
[성공 기준]
[Must 기능]
[Won't 기능]
[바이브/톤]
[참고 사이트]
[스택 선호]
```

## B. 슬라이스 작업 요청
```text
[이번 목표]
[건드리면 안 되는 것]
[관련 페이지/API]
[완료 기준]
[확인 방법]
```

## C. 버그 제보
```text
[증상]
[재현 단계]
[기대 결과]
[실제 결과]
[스크린샷/콘솔]
```

---

# 쇼핑몰(SSEUNG)에 대입한 미니 맵

당신이 이미 지나온 길을 이 문서 순서에 대입하면:

1. 목적: 패션 쇼핑 + 결제  
2. 범위: 상품/회원/장바구니/결제/관리자  
3. 동선: Home → Detail → Cart → Checkout → Orders / Admin  
4. 화면: 히어로, 상세 옵션, 관리자  
5. 스택: React + Express + MongoDB  
6. 데이터: User/Product/Cart/Order + admin 권한  
7. 바이브 구현: 기능 슬라이스 반복  
8. 연동: Cloudinary, PortOne  
9. 테스트: 로그인·장바구니·결제  
10. 배포: GitHub → Heroku → Vercel  
11. 개선: 다중 이미지 업로드, 인증 동기화 등  

즉, **바이브코딩으로 “완성한 것처럼” 보이기 전에  
설계 순서를 짧게라도 밟았기 때문에** 배포까지 간 것입니다.

---

# 마무리: 한 문장 정의

**웹사이트 설계 순서**란  
“예쁜 화면을 그리는 일”이 아니라  

> **누가, 무엇을, 어떤 순서로 하게 할지 정하고 →  
> 필요한 데이터와 권한을 정하고 →  
> 그에 맞춰 화면과 서버를 만들고 →  
> 배포해 확인하는 일**

입니다.

**바이브코딩**은 그 과정에서  
문서 대신 **대화와 실행**으로 속도를 내는 방법이고,  
용어를 알수록 AI에게 **정확한 지시**를 할 수 있습니다.

---

다음 추천 학습 순서:
1. 이 문서의 1~3단계를 새 아이디어에 직접 채워보기  
2. 페이지 5개짜리 사이트맵 만들기  
3. AI에게 “라우팅+빈 페이지”만 먼저 시키기  
4. 그다음 기능 슬라이스 하나씩

화이팅.
